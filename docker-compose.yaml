version: '3'
services:
  # postgres:
  #   image: postgres:latest
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #     - ./sql/init.sql:/docker-entrypoint-initdb.d/init.sql  # Mount the initialization script

  nugo-backend:
    image: nugomed/backend:1.4
    ports:
      - "8080:80"
volumes:
  pgdata:
